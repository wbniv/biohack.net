{
  "order":131,"slug":"farptrcmp","title":"Far-Pointer Order Shuffle","desc":"An insertion sort orders genuine 24-bit far pointers across a 64K bank boundary, then computes representable pointer differences between adjacent entries. Compiler stress-test #129 (Round 7).","keys":"Self-running — pointer lanes reorder across the bank boundary","category":"algorithms","pageTitle":"Far-Pointer Order Shuffle — bioHACK•NET","pageDesc":"A runnable SNES HiROM compiler probe for natural 24-bit far-pointer comparison and subtraction.","heading":"Far-Pointer Order Shuffle","lede":"Eight far pointers shuffle into address order while their adjacent cross-bank gaps drive the lower trace.","keysHtml":["Self-running — verified CRC <code>5B40</code>."],"doc":"<h2>The compiler path under test</h2><p>The target C source builds an array of genuine <code>address_space(2)</code> pointers, insertion-sorts it with natural pointer <code>&gt;</code>, and subtracts adjacent pointers. The data straddles a 64K boundary while keeping every difference representable by the ABI's 16-bit <code>ptrdiff_t</code>.</p><div class=\"rp-bugbox\"><p><b>Finding:</b> the first natural build exposed two missing InstructionSelect paths: an <code>Imag8</code>-backed <code>s8 to s1</code> truncation crashed the selector, and legal <code>Imag32 s32 to s16</code> truncation had no pattern.</p><p><b>Fix:</b> constrain the boolean source through the accumulator and select the low 16-bit <code>Imag32</code> subregister directly. Focused LLVM regressions plus the real-object machine verifier pass.</p><p><b>Result:</b> host, a16, and xy16 return <code>0x5B40</code> on MAME and bsnes-jg.</p></div><h2>What it shows</h2><p>The upper lanes show the sorted pointer bank order; the lower bars encode adjacent pointer gaps. A moving scan line keeps the verified result visibly alive.</p><p class=\"rp-foot\">Compiler stress-test #129, Round 7.</p>"
}
